install ubuntu and vagrant inside windows
open terminal in ubuntu vm
mkdir registration-app
cd registration-app
touch index.html script.js display.html
sudo apt update
sudo apt install apache2 -y

Start Apache:

sudo systemctl start apache2
STEP 5 — Write Code
1. index.html
<!DOCTYPE html>
<html>
<head>
    <title>Registration Form</title>
    <script src="script.js"></script>
</head>
<body>

<h2>User Registration</h2>

<form id="regForm">

    Name:
    <input type="text" id="name"><br><br>

    Email:
    <input type="email" id="email"><br><br>

    Password:
    <input type="password" id="password"><br><br>

    <button type="submit">Register</button>

</form>

<br>

<a href="display.html">View Registered Users</a>

</body>
</html>

Save file.

2. script.js
let users = JSON.parse(localStorage.getItem("users")) || [];

document.getElementById("regForm").addEventListener("submit", function(e) {

    e.preventDefault();

    let name = document.getElementById("name").value;
    let email = document.getElementById("email").value;
    let password = document.getElementById("password").value;

    let user = {
        name: name,
        email: email,
        password: password
    };

    // Push into array
    users.push(user);

    // Store in local storage
    localStorage.setItem("users", JSON.stringify(users));

    // AJAX POST Method
    let xhr = new XMLHttpRequest();

    xhr.open("POST", "https://jsonplaceholder.typicode.com/posts", true);

    xhr.setRequestHeader("Content-Type", "application/json");

    xhr.onreadystatechange = function() {
        if(xhr.readyState == 4 && xhr.status == 201) {
            alert("Registration Successful");
        }
    };

    xhr.send(JSON.stringify(user));

    document.getElementById("regForm").reset();

});

Save file.

3. display.html
<!DOCTYPE html>
<html>
<head>
    <title>Display Users</title>
</head>
<body>

<h2>Registered Users</h2>

<table border="1">

    <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Password</th>
    </tr>

    <tbody id="userdata"></tbody>

</table>

<script>

let users = JSON.parse(localStorage.getItem("users")) || [];

let table = document.getElementById("userdata");

users.forEach(function(user){

    let row = `
        <tr>
            <td>${user.name}</td>
            <td>${user.email}</td>
            <td>${user.password}</td>
        </tr>
    `;

    table.innerHTML += row;

});

</script>

</body>
</html>

Save file.
STEP 7 — Copy Files To Server Folder
sudo cp * /var/www/html/
STEP 8 — Open Browser In Ubuntu

Open:

http://localhost/index.html
